{{page-title "Channel"}}

<table class="channels">
    <tr>
      <th>Device Name</th>
      <th>Stereo/Mono</th>
      <th>type</th>
    </tr>
    {{#each @model as |device|}}
      <tr>
        <td>{{channel.audiotype}}</td>
        <td>{{channel.channeltype}}</td>
        <td>{{channel.channeldirection}}</td>
        <td>
          {{!-- <button
            type="button"
            {{on "click" (fn this.removeDevice device)}}
          >Remove</button> --}}
        </td>
      </tr>
    {{/each}}
  </table>
    
{{outlet}}