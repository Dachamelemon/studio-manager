
          <PowerSelect
            @options={{this.allChannels}}
            @placeholder="Please select a Channel"
            @selected={{this.selected}}
            @searchField="true"
            @onChange={{onchange}}
            as |channel|
          >
            {{channel.audiotype}}
          </PowerSelect>
          